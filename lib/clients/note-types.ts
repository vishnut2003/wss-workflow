export type ClientNoteListItem = {
  id: string;
  clientId: string;
  body: string;
  pinned: boolean;
  authorId: string;
  authorName: string;
  authorEmail: string;
  createdAt: string;
  updatedAt: string;
};
